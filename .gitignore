# evidência de trabalho: contém material interno, de cliente e pessoal.
# nunca versionar. o repositório é público (github pages).
.impeccable/evidence/

# protótipos descartáveis de direção visual
.impeccable/prototypes/

# config local da máquina
.impeccable/config.local.json
.impeccable/hook.cache.json

# verdade de produto fica local: contém a lista de claims não comprováveis,
# nota interna de cliente, número de processo e caminho de máquina. o
# repositório é público, e isso não é material de vitrine.
# DESIGN.md continua versionado: é sistema de design, e ser lido só ajuda.
PRODUCT.md

# build
dist/
.astro/
node_modules/
.env
.env.*
!.env.example
.DS_Store
